source "https://rubygems.org"

# Mirrors what GitHub Pages runs, so local previews match the live build.
gem "github-pages", group: :jekyll_plugins
gem "jekyll-include-cache", group: :jekyll_plugins

# Needed on Ruby 3.x
gem "webrick", "~> 1.8"
